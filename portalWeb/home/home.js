$(function () {
    var TallerAvanzada = {};
    var locacion = "http://" + window.location.host + "/biblioteca/";
    (function (app) {

        app.init = function () {
            app.bindings();
        };

        app.bindings = function () {


        };

        app.init();

    })(TallerAvanzada);


});

